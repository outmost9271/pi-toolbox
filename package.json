{
  "name": "pi-toolbox",
  "version": "0.2.1",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "description": "按全局默认值和项目覆盖启用集中管理的 Pi Skills 与命令行能力",
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "license": "MIT",
  "pi": {
    "extensions": [
      "./extensions/index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  }
}
